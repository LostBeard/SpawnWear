namespace SpawnWear.Config
{
    /// <summary>
    /// Template - copy this to <c>WifiCredentials.cs</c> (no .template suffix)
    /// and fill in your network's SSID + password. The destination file is
    /// gitignored so credentials never end up on GitHub.
    ///
    /// Phase 4 will replace this with BLE provisioning + persistent storage on
    /// the watch's flash so this manual step goes away.
    /// </summary>
    public static class WifiCredentials
    {
        public const string Ssid = "YOUR_NETWORK_NAME";
        public const string Password = "YOUR_NETWORK_PASSWORD";
    }
}
